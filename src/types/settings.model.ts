/** application design theme */
export enum Theme {
  Light = "light",
  Dark = "dark",
  System = "system",
}

/** Supported user languages in the interface */
export enum Language {
  EN = "en",
  DE = "de",
  PL = "pl",
}
