# Copy this file to .env and fill in the values
# VITE_ prefix makes variables available in the browser bundle

# Override the build version (defaults to git describe --tags --always)
# BUILD_VERSION=1.0.0
